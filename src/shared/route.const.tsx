import { AppUtils } from "@/shared";

export class ROUTES_CONST {
  public static CARRO = `${AppUtils.API_URL}/carro`;
  public static CARRO_MODELO = `${AppUtils.API_URL}/modelo`;
  public static CORES = `${AppUtils.API_URL}/cores`;
  public static PLANOS = `${AppUtils.API_URL}/plano`;
  public static CLIENTE_PLANO = `${AppUtils.API_URL}/clientePlano`;
  public static CARRO_CADASTRO = `${AppUtils.API_URL}/carroCadastro`;
  public static ESTACIONAMENTO = `${AppUtils.API_URL}/estacionamento`;
}
